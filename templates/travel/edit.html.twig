{% extends 'base.html.twig' %}

{#{% block title %} MODIFICATION VOYAGE {% endblock %}#}

{% block body %}
    <h1 class="text-center" style="width: 500px">MODIFICATION VOYAGE</h1>

    {{ include('travel/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-outline-dark mt-auto" href="{{ path('travel_index') }}">Retour à la liste</a>
    <div class="text-center"><a class="btn btn-outline-dark mt-auto"> {{ include('travel/_delete_form.html.twig') }}</a></div>
{% endblock %}
